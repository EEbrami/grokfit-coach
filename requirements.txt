# Core runtime dependencies for grokfit-coach Phase 1 (terminal agent).
# Prefer `pip install -e ".[dev]"` (see pyproject.toml) for development.
# This file is kept for compatibility with the original README instructions.

langgraph>=0.2.0
pydantic>=2.7.0
pydantic-settings>=2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
ollama>=0.3.0
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0
gradio>=4.0
